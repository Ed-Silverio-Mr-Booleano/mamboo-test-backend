
export interface TaskRequestEntity {
  name: string
  startDate: string
  finishDate: string
  status: string
}

export interface TaskResponseEntity {
  id: string
  name: string
  startDate: string
  finishDate: string
  status: string
}
